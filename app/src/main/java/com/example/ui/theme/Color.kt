package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Minimalist Black & White / Monochrome Palette
val SpaceBlack = Color(0xFF000000)      // Pure black background
val DeepSpace = Color(0xFF0A0A0A)      // Soft obsidian container surface
val NebulaCard = Color(0xFF141414)     // Metallic elevated cards

// Accent Monochrome Elements
val CyberCyan = Color(0xFFFFFFFF)      // Connected status marker (white)
val PlasmaBlue = Color(0xFFE5E5E5)     // Secondary actions (almost pure white / light silver)
val WarpGreen = Color(0xFFC0C0C0)      // Stable ping status (silver)
val WarningOrange = Color(0xFF888888)   // High latency (medium grey)
val DangerRed = Color(0xFF444444)      // Offline status (dark charcoal)

// Grey Text Tones
val StarryGrey = Color(0xFF9E9E9E)     // Cool medium grey
val NebulaGrey = Color(0xFFE0E0E0)     // Bright whitish-grey
